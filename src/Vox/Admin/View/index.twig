{% extends "Vox:Admin:layout" %}

{% block content %}

<div ng-controller="PostListCtrl">

<!-- content -->

<div class="col-sm-12" id="featured">
    <div class="page-header text-muted">
        {% trans "Posts" %}
    </div>
</div>

<!-- posts -->
<div class="row" ng-repeat="post in posts">
    <div class="col-sm-10">
        <h3><a ng-href="/admin/post/edit/{[{ post.id }]}">{[{ post.title }]}</a></h3>
        <h4><span class="label label-default">{[{ post.author }]}</span></h4>
        <div>{[{ post.excerpt }]}</div>
        <h4>
            <small class="text-muted">{[{ post.date }]} • <a ng-href="/admin/post/edit/{[{ post.id }]}" class="text-muted">{%  trans "Read More" %}</a></small>
        </h4>
    </div>
    <div class="col-sm-2">
        <a href="#" class="pull-right"><img ng-src="{[{ getAuthorPicture(post.author_id) }]}" class="img-circle"></a>
    </div>
</div>

</div>

{% endblock %}