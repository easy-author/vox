default:
    suites:
        front_features:
            paths: [ %paths.base%/features/front ]
        admin_features:
            paths: [ %paths.base%/features/admin ]
    extensions:
        Behat\MinkExtension:
            base_url: 'http://vox.mysite.com/'
            goutte: ~

travis:
    extensions:
        Behat\MinkExtension:
            base_url: 'http://localhost:8080/web'
